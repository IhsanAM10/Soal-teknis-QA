const { Given, When, Then } = require("@cucumber/cucumber");

Given("sistem memiliki sensor suhu", function () {
  // Inisialisasi kondisi sistem dengan sensor suhu
});

When("sensor mengukur suhu lingkungan", function () {
  // Simulasi pengukuran suhu lingkungan
});

Then("sistem dapat mendeteksi suhu lingkungan", function () {
  // Verifikasi bahwa suhu dapat terdeteksi
});

Given("sistem memiliki sensor kelembaban", function () {
  // Inisialisasi kondisi sistem dengan sensor kelembaban
});

When("sensor mengukur kelembaban udara", function () {
  // Simulasi pengukuran kelembaban udara
});

Then("sistem dapat mendeteksi kelembaban udara", function () {
  // Verifikasi bahwa kelembaban dapat terdeteksi
});

Given("sistem memiliki sensor intensitas cahaya", function () {
  // Inisialisasi kondisi sistem dengan sensor intensitas cahaya
});

When("sensor mengukur intensitas cahaya", function () {
  // Simulasi pengukuran intensitas cahaya
});

Then("sistem dapat mendeteksi intensitas cahaya", function () {
  // Verifikasi bahwa intensitas cahaya dapat terdeteksi
});

Given("sistem memiliki kamera untuk mengambil gambar", function () {
  // Inisialisasi kondisi sistem dengan kamera
});

When("sistem mengambil gambar kondisi perangkat serangga", function () {
  // Simulasi pengambilan gambar
});

Then("sistem dapat menyimpan gambar kondisi perangkat serangga", function () {
  // Verifikasi bahwa gambar telah disimpan
});

Given("sistem terhubung ke solar panel", function () {
  // Inisialisasi kondisi sistem terhubung ke solar panel
});

When("solar panel menghasilkan daya", function () {
  // Simulasi pengisian daya dari solar panel
});

Then("sistem dapat mengisi daya dari solar panel", function () {
  // Verifikasi bahwa sistem dapat mengisi daya
});
